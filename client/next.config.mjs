/** @type {import('next').NextConfig} */
const nextConfig = {
	images: {
		domains: [
			"plus.unsplash.com",
			"images.unsplash.com",
			"cdn.sanity.io",
			"img.evbuc.com",
			"cdn.evbuc.com",
		],
	},
};

export default nextConfig;
